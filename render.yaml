services:
  - type: web
    name: mental-health-api
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn api_rest:app
    envVars:
      - key: OPENAI_API_KEY
        sync: false
      - key: PYTHON_VERSION
        value: 3.12.0
    healthCheckPath: /health
